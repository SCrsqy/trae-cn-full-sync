# =====================================================
# 优化版系统依赖
# 新增以下依赖
# =====================================================

# 核心依赖(原方案已有)
# pandas>=1.3.0
# openpyxl>=3.0.0
# psycopg2-binary>=2.9.0
# redis>=4.0.0
# flask>=2.0.0
# lunar-python>=1.0.0
# selenium>=4.0.0
# apache-airflow>=2.5.0

# 新增依赖
prometheus-client>=0.17.0      # Prometheus指标暴露
requests>=2.31.0               # HTTP客户端(用于智能爬虫)
urllib3>=2.0.0                  # 请求库依赖

# 可选: 异步支持
# redis[hiredis]>=4.0.0        # 更好的Redis性能
# celery>=5.0.0                # 分布式任务队列
